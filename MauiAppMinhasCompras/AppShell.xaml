<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="MauiAppMinhasCompras.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MauiAppMinhasCompras"
    xmlns:Views="clr-namespace:MauiAppMinhasCompras.Views"
    Title="MauiAppMinhasCompras">

    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate Views:ListaProdutos}"
        Route="MainPage" />

    <!-- Rotas para login e cadastro de usuário -->
    <ShellContent Title="Login" ContentTemplate="{DataTemplate Views:LoginPage}" Route="Login" />
    <ShellContent Title="CadastroUsuario" ContentTemplate="{DataTemplate Views:CadastroUsuario}" Route="CadastroUsuario" />

</Shell>
